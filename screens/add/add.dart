import 'package:flutter/material.dart';

class addscreen extends StatelessWidget {
  const addscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('addscreen'),),
    );
  }
}